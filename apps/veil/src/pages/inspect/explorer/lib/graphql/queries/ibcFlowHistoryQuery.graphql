query IbcFlowHistory($clientId: String, $days: Int) {
    ibcFlowHistory(clientId: $clientId, days: $days) {
        date
        inflowVolume
        outflowVolume
        inflowCount
        outflowCount
    }
}
